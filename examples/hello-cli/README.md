# Hello CLI Example

This example will demonstrate composing a simple command-line component via `composectl`. Populate this folder with the plan (`plan.cbor`), optional source WIT, and build scripts as they become available.
