# Tooling

Developer-facing utilities and CLIs are stored here. The `composectl/` crate will orchestrate plan validation, emission, and execution while sharing logic with the host runtimes.

Document installation instructions and CLI surface area as the implementation stabilizes. Keep generated completions and binaries out of source control.
